///Rift_LockCursor( x, y, width, height )

/* Locks the mouse inside of a rectangle on screen.  You can use this to constrain the mouse
to the game window while wearing the Rift. */

RiftExt_LockCursor(argument0, argument1, argument2, argument3);

